export function getNextLengthToBePlaced() {
  // let imageList = document.querySelectorAll('img')
  let lastPlacedPicture = document.querySelector("[last-placed-picture]");
  switch (lastPlacedPicture.id) {
    case "x-wing":
      break;

    default:
      break;
  }
}
